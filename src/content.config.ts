/**
 * src/content/ contiene moduli TypeScript tipizzati (vedi src/content/types.ts),
 * non "content collections" di Astro: qui dichiariamo esplicitamente che non
 * ci sono collections, così Astro non tenta di auto-generarle.
 */
export const collections = {};
